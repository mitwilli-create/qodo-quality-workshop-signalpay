# Retired Hosted-Review Setup Build Plan

Status: retired on 2026-08-09.

This path remains only as a historical marker. Do not execute the former setup
steps. Qodo is fully halted, and this workshop must not install, authenticate,
connect, or invoke a hosted reviewer.

Use `AGENTS.md`, `rules/README.md`, the local skills under `skills/`, and
`make verify` for the current workflow.
